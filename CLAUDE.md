# 給協作 AI 的專案說明

這份文件會被自動載入，開始動手前請先讀完。對外的專案介紹在 [README.md](README.md)，這裡放的是維護時真正需要知道的事。

## 專案性質

保全法扣作業的公文套版工具，單一 `index.html`，無建置流程，純前端，零對外連線，透過 GitHub Pages 部署。所有樣式、頁面與 JavaScript 都在同一個檔案裡。

使用者是實際承辦法扣業務的人，不是工程師。提出的需求幾乎都來自真實案件，**當使用者說「實務上是這樣寫」時，那就是規格**，不要用一般公文常識去推翻。

## 與使用者協作的規則

- **不要自行 `git push`**。改完先請使用者實測，得到確認才推。push 等於直接上線給同事使用，這個工具目前在公司資安檢視中，線上出錯代價高。commit 到本機沒問題
- **不要憑空推導業務規則**。公文寫法、扣款邏輯、作業流程都問使用者，不要自己「合理推測」後直接寫進程式
- **重要邏輯與決策要寫進這份文件**。之後會有其他 AI 接手，交接靠的是這裡
- **程式碼與文件不得出現任何公司資訊**（單位名稱、地址、電話、實際文號）。寄件人資訊一律由使用者在介面上自行設定，存 localStorage

## 慘痛教訓：不要自己腦補座標

信封版型曾經花了好幾天，原因是「試圖重現 Word 的排版引擎」而不是「去讀實際排版結果」。

**版型座標的唯一正確來源是範本 PDF，不是 docx。** docx 存的是相對座標：

```xml
<wp:positionH relativeFrom="column"><wp:posOffset>-451440</wp:posOffset></wp:positionH>
```

它只說「相對於某參考點往左 12.54mm」，而那個參考點在紙上的絕對位置**檔案裡沒有寫**，必須重現 Word 的排版引擎才能得出（段落由右往左的推進量、行高與網格對齊、直書時的邊界旋轉⋯）。曾經據此推導出整組座標，實測結果：

- 垂直方向整組偏差約 **45mm**（誤把直書上邊界當成 `pgMar right`=20mm，實際應為 `pgMar top`=60mm）
- 水平方向差 1~16mm
- 另一次嘗試「模擬信封置中送紙、把座標左移 38.5mm 同時把紙張設成 120mm 寬」，等於偏移套用兩次，實測整體偏左 38.5mm

**正確做法**：用 Word 把 docx 另存成 PDF，從 PDF 讀座標。PDF 存的是 Word 已經排版完成的絕對座標，是事實而非推導。改用這個方法後，六個區塊一次到位、零誤差。

這個教訓可以推廣到整個專案：**能量到的就不要算**。

### 若日後範本異動，維護步驟

1. 用 Word 開啟新範本 → 另存新檔 → PDF
2. 用 PyMuPDF 解析（本機已安裝，`import fitz`）：

```python
import fitz
d = fitz.open('範本.pdf'); p = d[0]
PT2MM = 25.4/72
for blk in p.get_text('dict')['blocks']:
    for ln in blk.get('lines', []):
        for sp in ln['spans']:
            x0,y0,x1,y1 = [v*PT2MM for v in sp['bbox']]
            print(sp['text'], sp['font'], sp['size'], x0, y0, x1, y1)
```

3. 依中心線分欄（同一直書欄的字，x 中心相近），取每欄的「中心線距紙張右緣」與「文字頂端 y」
4. 更新 `ENV_BASE` 常數
5. 若基準有變動，記得把 `localStorage` 的 key 升版（目前是 `envCalibV4`），讓使用者已存的舊校正值自動失效

**判讀 PDF 數值時的陷阱**：字元的 bbox 不等於文字方塊位置。例如寄件人第三欄量到的頂端比另兩欄高 4mm，但那只是因為該欄首字是橫躺的 Arial 數字、bbox 起算方式與中文不同，方塊本身其實是齊頭的。照抄前要先判斷那個數字代表什麼。

### 目前的版型基準（取自範本 PDF 實測）

紙張 200×270mm。五個直書欄位，以「中心線距紙張右緣」定位：

| 欄 | 內容 | 中心線 | 文字頂端 | 字級 |
|---|---|---|---|---|
| 1 | 郵遞區號 | 56.00 | 68.96 | 22pt（**橫書**） |
| 1 | 地址（同欄下方） | 56.00 | 87.14 | 18pt |
| 2 | 收件人姓名＋稱謂＋收 | 98.20 | 97.72 | 26 / 22 / 24pt |
| 3 | 寄件人單位＋專員 | 145.27 | 135.31 | 12.9pt |
| 4 | 寄件人地址 | 151.81 | 135.31 | 12.9pt |
| 5 | 公文編號＋電話分機 | 158.15 | 135.31 | 12pt |

- 定位方式：`right` 把元素右緣放到中心線，再 `translateX(50%)` 推回，使元素中心落在該線
- `line-height: 1` — PDF 實測 18pt 地址的字距正好等於字級 6.35mm
- 姓名字距 `letter-spacing: 1em` — PDF 實測 26pt 姓名字距 18.38mm，約字級的兩倍

### 收件人的兩種排法（以 4 字為界）

範本量到的 26 / 22 / 24pt 階層是為「個人姓名＋稱謂＋收」設計的，套到機關全銜會壞掉，故分兩種：

| 收件人 | 排法 |
|---|---|
| 4 字以內（個人姓名，如解約件的債務人） | **完全照範本**：單欄，姓名 26pt ＋字距，稱謂 22pt，收 24pt。已實測過，不要動 |
| 超過 4 字（機關全銜） | 收件人與聯絡人**各自成欄、各自定位**，統一 22pt |

機關模式的細節：

- 聯絡人欄的 `cx` ＝ 收件人欄 ＋ 一個字寬，排在左邊（直書由右往左）
- 聯絡人欄的 `top` ＝ 收件人欄 ＋（稱謂欄前的空格數 × 一個字高）。使用者用空格微調高度
- 統一 22pt 有兩個理由：26pt 的長全銜一欄放不下；機關與聯絡人是**平行關係**不是主從，字級不同會讓人覺得「這裡出錯了」

### 教訓：不要用文字去控制排版

機關收件人的排版改了三次才對，前兩次都失敗，原因是同一個：**想用文字內容去控制版面位置**。

1. 先靠自動折行斷句 → 折行點由字數決定、不管語意，把「黃小姐」拆成「⋯黃」「小姐」
2. 想用空格把稱謂往下推 → **行首的空白字元一律會被瀏覽器丟掉**（半形、全形都一樣），而折行點剛好就落在稱謂前面
3. 改用 `padding-top` → 仍然綁在同一個會被折行切開的行內盒子上

正解是**各自成為獨立的絕對定位元素**，位置用座標直接指定。這是信封座標那個教訓的延伸：**能直接指定的就不要靠推導**，版面位置尤其如此。
- **郵遞區號必須用橫書**：範本該方塊 `vert=horz`。若誤套直書規則，超過 4 碼會觸發橫躺而往下延伸撞到地址欄
- `ENV_PRINT_OFFSET_Y = -20` 是**人工的版面微調**：PDF 座標照抄後，整體看起來偏低，故往上移 20mm。這是肉眼判斷的結果，不是量出來的，**與印表機無關** —— 換印表機不需要動這個值，印表機造成的偏移請走介面上的校正欄位。刻意與 `ENV_BASE` 分開記錄：範本更新時 PDF 座標照抄即可，此值再另行以肉眼判斷
- 信封校正預設值經台中使用者實際列印確認：紙張 200×270mm；郵遞區號／地址 `dx=-6, dy=-20`；收件人 `dx=-1, dy=-15`；寄件人／文號 `dx=3, dy=0`。使用者自行儲存過的校正仍優先；按「恢復預設校正值」才會回到這組數值
- 「顯示測試參考框」已移除，信封列印一律直接輸出實際郵遞區號與內容
- 郵遞區號與地址雖然初始校正值相同，但必須保留各自的 `dx`、`dy` 欄位，讓使用者能分開微調；舊版只有地址校正值時，郵遞區號先沿用該值，避免更新後位置跳動

### 直書中的數字排版

規則取自範本 PDF 的字元 bbox 實測，見 `rotateNums()`：

| 情況 | 處理 | 判讀依據 |
|---|---|---|
| 4 碼以內（門牌號等） | `text-combine-upright: all` 壓進一個字寬 | 地址「23」兩字並排、總寬 6.35mm 恰等於 18pt 一個字寬 |
| 超過 4 碼（公文編號、電話、分機） | `text-orientation: sideways` 整串橫躺 | 每個字元 x 固定、y 遞增，寬 5.77mm 等於字高（即旋轉 90°） |

字型：公文編號與電話號碼在範本中是 **Arial**，分機號碼則維持**標楷體**，故 `rotateNums()` 有 `sidewaysFont` 參數。

**不要改用 `transform: rotate()`**：transform 不影響版面配置，只影響繪製，旋轉後的文字會覆蓋旁邊內容（需額外用 canvas 量寬度補高度才能避免）。CSS 原生的 `text-orientation` 才是正解。

## 業務邏輯規則

這些是使用者依實務給的規格，**不要自行修改**。要改請先問。

### 共通

- 對內備註格式：`作業日，給付日，函覆（公文文號）＋所選說明`。作業日是實際給付日的**前一工作天**（`previousWorkday()`，只避開週末，不處理國定假日）
- 對內備註自動產生，但使用者一旦手動編輯過就停止自動覆蓋（`internalNoteManuallyEdited` / `ntNoteEdited` 旗標）
- 公文文號共 15 碼英數，在填寫頁輸入後自動帶入對內備註與信封
- 日期欄位預填「當年當月」但不填日，例如 `115年8月日`，留「日」給使用者補
- 手續費預設 250 元
- 說明段落編號**必須連續**。若中間某點沒選，後面的點會自動遞補（第三點的內容在沒有第二點時會排成「二、」），這是刻意的
- 解約與非解約的「密等提醒」只放在「公文預覽」分頁最上方，不放在填寫頁
- 若保單／債權人已選匯款或支票，但對應日期或必要資料尚未完整，在原操作區與公文預覽都要提醒，並提供「前往／返回填寫」按鈕；不強制阻擋預覽

### 解約件

- 每張保單只能有一種給付方式；外幣保單固定匯款，新臺幣保單可選匯款或支票
- 同一案件可同時有匯款與支票。公文不另造新文句，而是把原有的匯款內容與支票內容合併在同一點；附件清單也同時列出匯款紀錄與支票影本
- 附件清單放在公文預覽中，位於公文內文之後、明細表之前。解約件固定列解約明細表；非解約件不列解約明細表
- 非解約附件依實際給付方式顯示：總公司照會公文與執行命令固定為對內附件；有匯款才增加對內的系統匯款紀錄截圖；有支票才增加對內與對外的支票影本；只有匯款時顯示「本件無對外附件」
- 解約日原則上是匯款日或開票日的前一工作天（程式僅排除週末，國定假日由使用者確認）。若匯款與開票對應的解約日不同，對內備註分開列明
- 混合給付的對內備註依解約日先後排列，並將同一交易放在一起：「支票保單解約 → 開票 → 收／寄支票日」、「匯款保單解約 → 匯款」
- 解約與非解約只要使用支票，都要分別提供「收支票日期」與「寄出支票日期」，兩者可能不同，不得合併成同一日期
- 收支票與寄出支票日期預帶當年、當月，讓使用者只補日期；對內備註的日期一律使用斜線格式。尚未補日顯示如「115/9/○」，填完後顯示如「115/9/10 收支票，115/9/11 寄出支票」
- 退匯屬例外情況：使用者依最後實際處理結果更改給付方式與日期，必要時手動修改對內備註，程式不自行猜測退匯後流程
- 手續費跟著債務人，**同一件最多只扣一次**（約 95% 案件需扣）。第一張新臺幣保單預設 250；案件已有新臺幣保單時，後續新增保單的手續費預設 0，欄位可手動改
- 哪張保單填了手續費，明細表註記就帶入該張保單號碼；公文須寫出實際金額（例如「已扣除手續費250元」）
- 一般原則是有新臺幣保單時，250 元手續費優先由新臺幣保單扣除；全為外幣時由使用者輸入 250 元換算後的外幣金額，可含小數。這是提醒原則，不授權程式自動搬移已輸入金額
- **已輸入的手續費永遠優先於一般原則**：新增、刪除保單或改變幣別時，程式不得自動清除、搬移或覆蓋任何已輸入的手續費；只能提醒一般優先扣新臺幣與同案最多扣一次，由使用者依實際作業結果確認
- 新增新臺幣保單時，若案件已有任何手續費，新保單手續費預設 0；只有案件全部尚無手續費且原本沒有新臺幣保單時，才預設 250
- 解約件也可不扣手續費，不可用程式強制。實務上不扣多因金額不足，且解約目前規定金額至少超過 14.9 萬元才執行；此為背景，未授權加入硬性金額驗證
- 多幣別案件的合計實領金額必須依幣別分開加總與顯示，不得將不同幣值相加，也不得只顯示「多幣別」
- 所有幣別的公文金額格式一致：「新臺幣33,445元」、「美元33,445元」、「澳幣33,445元」、「人民幣33,445元」，不得寫成「人民幣33,445人民幣」
- 多幣別案件的公文必須在手續費前列出實際扣款幣別（例如新臺幣保單扣 250 元寫「已扣除手續費新臺幣250元」；美元保單扣款則寫「已扣除手續費美元○元」）。單一新臺幣案件可簡寫為「已扣除手續費250元」
- 尚未填寫解約金的保單不列入畫面與公文合計，避免尚未輸入就顯示「某幣0元」；使用者明確輸入 0 時仍列入
- 保單明細的第一排固定為「左：保單號碼／右：險別名稱」，這是使用者依實際作業順序確認的排列
- 案件基本資料順序固定為「回函公文文號 → 債務人姓名 → 來文法院／執行機關 → 執行命令日期／字號」；回函文號只提示共 15 碼，不得寫入公司內部文號前綴
- 上述案件基本資料名稱與順序，解約件與非解約件必須一致
- 明細表註記只有一則時顯示「註：」不編號 —— 單獨一個「註1」會讓人誤以為漏寫了後續項目
- 第三點預設「依函揭命令說明○未予控管」
- 第三點可選「●君之保險契約依令終止後，於本公司已無有效保單，併予敘明。」，其中●自動帶入債務人姓氏
- 解約金明細表只放在「公文預覽」，並在該處直接列印；「郵寄列印」不得再放一份重複表格

### 非解約給付

無明細表，但**有信封**（支票會寄給收款人，且採雙掛號）。改為「多債權人各一筆金額、各自匯款或支票」。主旨組句規則：

- **填寫順序固定為**：案件基本資訊 → 給付項目 → 給付對象、實際給付金額及給付方式 → 依給付方式顯示的日期 → 第二點 → 第三點 → 對內備註
- 每筆給付對象可選「債權人」或「自填」：債權人名稱可不填，未填時公文統稱「債權人」；自填供法院、移送機關或其他對象使用，名稱必填並直接帶入公文
- 新增給付對象時不預選給付方式，須由使用者選擇；未選時在該筆資料內及公文預覽提示
- 日期欄位依實際選擇顯示：只有匯款就只顯示匯款日期、只有支票就只顯示開票日期、混合給付才同時顯示兩者；切換方式只隱藏未使用欄位，不清除已輸入日期
- 日期尚未填完整時，只在日期欄位下方顯示簡短提示，不在同一區塊放置多餘的「前往填寫」按鈕；公文預覽仍可提醒並返回填寫

| 情況 | 輸出 |
|---|---|
| 全部匯款 | `有關債務人○○○之【類型】債權，本公司已於【日期】匯款新臺幣【各筆金額】元至【對象】指定帳號【手續費】，請查照。` |
| 全部支票 | `隨函檢附債務人○○○之【類型】債權支票影本【張數】【手續費】，請查照。` |
| 匯款與支票並存 | 以「有關⋯」起首，兩段以「，並」相接 |

- **給付類型**：可複選。三項以上時前面用頓號、只有最後一項用「及」（`返還保價金、紅利保險金及滿期保險金`）。第五項「已得請領保險金」是多種債權並存時的統稱，第六項「自填」供例外情形
- **給付項目介面**：生存、滿期、返還保價、紅利四項以大型勾選方塊常時顯示並可複選；「已得請領保險金」是依法院來文使用的統稱，啟用後個別項目不寫入公文，但必須保留原勾選，切回時恢復；自行填寫另區顯示
- **收款對象**（`ntPayeeText`）：自填的法院、移送機關等名稱直接帶入；有填債權人名稱就列名稱，未命名債權人用人數統稱，**但兩人以內不寫數字**，直接寫「債權人指定帳號」，三人以上才寫「三債權人」
- **張數量詞**：單張寫「乙紙」（公文慣例），多張才用數字
- 非解約公文中「債務人」與姓名之間固定加入半形斜線 `/`，例如「債務人/王大明」，供內部辨識本件不用寄給債務人；解約件不套用
- 更正先前函覆金額時，原告知金額與更正後金額都要自動加千分位
- 非解約主旨中的每一筆實際給付金額也要自動加千分位；多筆時各自格式化後再用頓號連接
- 非解約第二點共有三種寫法，預設為「敬請鈞院／署准予撤銷本件扣押命令並副知本公司，實感德便。」；另兩種為「依函揭命令說明○未予控管」與「依函揭命令主旨、說明○未予控管」，都以所選法院／執行機關及使用者填入的點次組句
- **手續費 250 元跟著債務人**，不是跟著債權人，所以同一件只扣一次。給付金額不足以扣手續費時，勾選「不扣手續費」會把「（已扣除手續費250元）」整段拿掉
- 非解約的債權人金額欄一律填「已扣除手續費後，實際匯款或實際開票的金額」；畫面總額須寫「已扣除手續費250元」，不得寫成會讓人以為還要再減一次的「另扣手續費」
- 第二點預設「敬請鈞院於本案執行命令撤銷後，另行副知本公司俾便結案」
- 第三點放「更正先前函覆金額」與自填內容
- **信封收件人可能是法院／執行機關，也可能是多個債權人**。實務上兩者不會並存，但那是作業上的事實，**不需要用介面去強制** —— 曾經做過「寄送對象」切換，但它不影響任何輸出，只是逼使用者做一個不改變結果的決定，已移除。收件人就是一份可自行增減的清單，一次輸出多張、每張各自成頁
- 「⟳ 帶入領支票的債權人」是明確的動作按鈕，只在真的有領支票的債權人時出現，數量直接寫在按鈕上。原本這是切換的副作用且只在清單全空時生效，等於一直是死的
- 版面與校正值和解約件共用同一套（`printEnvelopes()` 收陣列，解約件傳單一元素）
- 解約與非解約共用同一份自存常用收件人；兩邊都可選取、新增、管理，只帶入名稱、郵遞區號與地址，不自動改變稱謂
- 設定備份入口在解約與非解約信封頁都要看得到，開啟同一個共用視窗
- 寄件人設定、信封版面校正及設定備份必須在解約與非解約信封頁呈現相同介面；程式使用同一個實際 DOM 區塊依情境移動，不建立兩份容易不同步的設定欄位
- **回執聯套印**：紙張與座標只依 `回執聯-最新範本.pdf` 實測，不得猜測；帶入內容維持原規則，只更新實測座標。非解約件依信封收件人一人一張；解約件供掛號退回後第二次改寄雙掛號時手動列印
- 回執聯範本是把整行橫式文字旋轉 90 度後印在紙上，中文字本身也必須跟著旋轉；不得做成每個中文字保持正向的直式排版
- 回執聯帶入收件人郵遞區號、地址、姓名／機關名稱及案件的「執行命令字號」，不得誤用回函公文文號；寄件人資料沿用本機寄件人設定
- 寄件人設定只填專員姓氏，信封與回執聯一律顯示「姓氏＋專員」，不得預設先生或小姐；若使用者已輸入「專員」不得重複附加
- 解約與非解約信封的稱謂都使用可自行輸入、可留空的欄位；寄法院或移送機關時不得強迫選先生或小姐
- 解約與非解約信封的稱謂介面一致，提供「先生／小姐／不加稱謂／自行填寫」四個按鈕；自行填寫才展開文字欄。稱謂位置以「－／＋」調整往下幾個字，不再要求使用者自行輸入看不見的前置空格
- 稱謂欄位旁必須直接提醒「收件人超過 4 字時，稱謂會自動換到左側」，不可只說可以調整位置，否則使用者無法預先知道列印結果
- 郵寄列印頁的公文字號屬於本案郵寄資料，解約件放在收件人地址下方；非解約件因可能有多位收件人，只顯示一個共用欄位，放在全部收件人資料之後、列印按鈕之前。不得放到寄件人、校正或備份設定之後

### 待補：郵局回執聯

非解約給付寄支票採**雙掛號**，需要套印**郵局回執聯**。這是**尚未實作**的功能，卡在缺少範本。

**取得座標的方式與信封完全相同，不要自行推導**：拿實際的回執聯表單，用同樣的方法量出絕對座標（見上面的信封那節）。在拿到範本之前不要寫任何座標進程式。

### 尚未實作的情境

清算件、介入權、縮小保額、綜合件。其中**綜合件＝解約＋非解約的組合**，可重用兩者的邏輯。

## 關鍵函式

| 功能 | 位置 |
|---|---|
| 信封版面（列印用，唯一來源） | `buildEnvelopeHTML(data, calib)` |
| 版型基準常數 | `ENV_PAPER`、`ENV_BASE`、`ENV_PRINT_OFFSET_Y` |
| 校正值存取 | `loadEnvCalib` / `getEnvCalib` / `saveEnvCalib` / `resetEnvCalib`（localStorage key：`envCalibV4`） |
| 直書數字處理 | `rotateNums(s, sidewaysFont)` |
| 解約件對內備註 | `buildAutoInternalNote()` / `updateInternalNoteAuto()` |
| 解約件明細表 | `generatePreview()`（公文預覽與列印的唯一來源） |
| 金額防呆 | `validateAmounts()` / `renderAmountWarnings()` |
| 非解約給付主旨 | `ntBuildSubject()` / `ntPayeeText()` / `ntTypeText()` |
| 非解約給付說明段落 | `ntBuildNotes()`（回傳已編號的陣列） |
| 非解約給付對內備註 | `ntBuildNote()` |
| 一鍵清空 | `clearCase()` / `clearFieldsIn()` |
| 常用收件人 | `ENV_PRESET_ORGS`（內建，寫死）／ `loadCustomOrgs` / `saveCustomOrgs`（localStorage key：`envOrgsV1`） |
| 設定備份／還原 | `downloadSettingsBackup()` / `restoreSettingsBackup()`（僅寄件人、信封校正、自存常用收件人） |
| 共用複製邏輯（含 Word 標楷體） | `copyRichText(text, btnId, btnLabel)` |

命名慣例：非解約給付的所有識別字一律加 `nt-` / `nt` 前綴，與解約件的邏輯完全隔離，避免兩情境互相牽動。

## 設計決策與理由

- **金額防呆採提示而非阻擋**：公文金額情況多，硬性擋掉會卡住作業；且輸入過程中金額本來就會短暫不完整
- **已移除「產出 Word 檔」**：需從 CDN 載入 docx.js，與「離線可用、零對外連線」的定位衝突且經常失敗
- **已移除信封預覽**：與實際列印有落差，改以瀏覽器的列印預覽為準
- **複製公文必須用 `<p>` 分段**：Word 會忽略單一區塊內的 `<br>`，導致全部擠成一段。中文要套標楷體則需 `mso-fareast-font-family` 等 Word 專屬屬性
- **`navigator.clipboard` 要先檢查存在**：只在安全內容（HTTPS）下才有，`file://` 或 HTTP 會是 undefined，需 fallback 到 `execCommand`
- **一鍵清空放在標題列**：清空的時機有兩個 —— 複製完公文要接下一件、以及填到一半發現整件錯了。放公文頁只涵蓋前者。標題列是「整件」層級的動作區，且與「複製公文」「列印」等主要按鈕保持垂直距離，降低誤觸
- **清空用前綴白名單**：`clearCase()` 會清掉情境區塊內所有欄位，只保留 `KEEP_PREFIXES`（`set-` 寄件人設定、`env-cal-` 信封校正）。用白名單而非逐一列舉，新增欄位會自動被清到。新增欄位時若屬於「設定」而非「案件資料」，記得把 id 前綴加進白名單
- **設定備份不得收入案件資料**：備份 JSON 只包含寄件人、信封校正與使用者自存的常用收件人；債務人、保單、金額、法院案件與回函文號都不得進入備份。備份與還原全程在瀏覽器本機完成
- **內建的法院地址一定要使用者確認過才寫進 `ENV_PRESET_ORGS`**：地址錯了就是雙掛號的支票寄丟，代價遠高於省下的輸入時間。**不要憑記憶或從網路抓**。使用者自存的常用收件人走 localStorage，與內建清單分開管理
- **不要拿 `.secondary-btn` 當小按鈕用**：它是 `width:100%` 的整頁寬按鈕，塞進一列裡會把旁邊的欄位擠爛。欄位旁的小動作按鈕請用 `.mini-btn`
- **欄位標籤要能維持單行**：說明文字太長會把標籤撐成多行，同一列的另一個欄位就對不齊。長一點的說明放到區塊下方的提示框，不要塞進 `.field-label`
- **不改變輸出的模式切換不要做**：一個切換若不影響印出來或複製出來的東西，它就只是在逼使用者做一個不影響結果的決定。是「動作」就做成按鈕（隨時可按、可標數量），不要做成「狀態」
- **「關於」只放在首頁**：工具頁原本有 ℹ 按鈕，但首頁反而看不到說明，位置不對，已移除
- **離線限制使用 CSP 強制**：主頁與列印視窗的 `connect-src` / `default-src` 皆為 `none`，且所有會進入 `innerHTML` 的使用者輸入都必須先經 `escapeHtml()`；新增欄位時不得直接插入 HTML 字串

## 開發／測試注意事項

- **瀏覽器會恢復表單狀態**：重新整理時 `<select>` 會跑回上次的選擇，蓋掉 HTML 的 `selected`。預設值因此一律在初始化時用 JS 明確指定。測試時若「改了沒生效」，先確認是不是這個
- **`openTool()` 不會重置狀態**：`policies`、`ntCreditors`、`ntSelectedTypes` 都留在記憶體，從首頁重進也不會清。這是刻意的（切換情境不該弄丟資料），要清請用 `clearCase()`。**測試時務必注意上一輪的殘留值**，否則會誤判結果
- **列印字級比畫面大**（11pt vs 11px），欄寬在畫面上正常不代表列印正常。測試表格排版要模擬實際 A4 尺寸與列印 CSS
- **列印視窗會被瀏覽器封鎖**（尤其 `file://`），已加提示訊息，不再無聲失敗
- **版本號需手動維護**：`APP_VERSION` 與 `APP_UPDATED` 兩個常數，改版時記得同步更新，否則首頁與「關於」顯示的日期不會變。「關於」裡的更新紀錄也要一併補上，**只留最近四筆**
- 範本 docx／PDF 與座標分析暫存檔都在 `.gitignore` 中，不上傳公開 repo（含公司資訊）
